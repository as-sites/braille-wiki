# Use DecapBridge PKCE auth (required)
backend:
  name: git-gateway
  repo: as-sites/braille-mkdocs
  branch: main
  auth_type: pkce
  base_url: https://auth.decapbridge.com
  auth_endpoint: /sites/053d26e7-e7c8-46eb-9a02-bb6dd1e168c0/pkce
  auth_token_endpoint: /sites/053d26e7-e7c8-46eb-9a02-bb6dd1e168c0/token
  gateway_url: https://gateway.decapbridge.com

  # See who did what (optional)
  commit_messages:
    create: Create {{collection}} “{{slug}}” - {{author-name}} <{{author-login}}> via DecapBridge
    update: Update {{collection}} “{{slug}}” - {{author-name}} <{{author-login}}> via DecapBridge
    delete: Delete {{collection}} “{{slug}}” - {{author-name}} <{{author-login}}> via DecapBridge
    uploadMedia: Upload “{{path}}” - {{author-name}} <{{author-login}}> via DecapBridge
    deleteMedia: Delete “{{path}}” - {{author-name}} <{{author-login}}> via DecapBridge
    openAuthoring: Message {{message}} - {{author-name}} <{{author-login}}> via DecapBridge

# Enable PKCE fields (optional, recommended)
auth:
  email_claim: email
  first_name_claim: first_name
  last_name_claim: last_name
  avatar_url_claim: avatar_url

# Better Decap + Bridge logo (optional)
logo_url: https://decapbridge.com/decapcms-with-bridge.svg

# Add site links in Decap CMS (optional)
site_url: https://braille.ay.ms

media_folder: docs/assets/images
public_folder: /assets/images

collections:
  - name: home
    label: Home
    files:
      - name: home
        label: Home Page
        file: docs/index.md
        fields:
          - { label: Body, name: body, widget: markdown }

  - name: ueb
    label: UEB
    folder: docs/ueb
    create: true
    extension: md
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Body, name: body, widget: markdown }

  - name: nemeth
    label: Nemeth
    folder: docs/nemeth
    create: true
    extension: md
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Body, name: body, widget: markdown }

  - name: reference
    label: Reference
    folder: docs/reference
    create: true
    extension: md
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Body, name: body, widget: markdown }
