# docker-compose.yml
# Skeleton configuration — filled in by plan 14 (Infrastructure)

services:
  # web:
  #   build: ./apps/web
  #   restart: unless-stopped
  #   environment:
  #     - DATABASE_URL
  #     - INTERNAL_SECRET
  #     - PUBLIC_API_URL

  # api:
  #   build: ./apps/api
  #   restart: unless-stopped
  #   environment:
  #     - DATABASE_URL
  #     - BETTER_AUTH_SECRET
  #     - INTERNAL_SECRET
  #     - R2_ACCOUNT_ID
  #     - R2_ACCESS_KEY_ID
  #     - R2_SECRET_ACCESS_KEY
  #     - R2_BUCKET_NAME

  # admin:
  #   build: ./apps/admin
  #   restart: unless-stopped

  # traefik:
  #   image: traefik:v3
  #   restart: unless-stopped
  #   ports:
  #     - "80:80"
  #     - "443:443"
